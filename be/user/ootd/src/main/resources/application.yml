spring:
  application:
    name: user-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_db?autoReconnect=true&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 0oTd!

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQLDialect
      format_sql: true

  doc:
    default-consumes-media-type: application/json
    default-produces-media-type: application/json
    api-docs:
      path: /v3/api-docs
    swagger-ui:
      path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

  instance:
    hostname: localhost
    prefer-ip-address: false

server:
  port: 8081

jwt:
  secret: e13af114d471424d6c9f2b7d03c3e25e630f269fbb3587f35068cb7d253150f568812b114afd8fd6941a005cdcf64b8b8baec4ce9de512b6aaad9ba732c6eee4  # JWT ?? ?
  expiration: 1296000000  # JWT ?? ?? (??? ??)

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework: DEBUG